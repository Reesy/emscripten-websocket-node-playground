{
  "name": "client",
  "version": "1.0.0",
  "description": "The include/emscripten folder only exists for intellisense purposes, used libraries are already discovered by the emcc compiler.",
  "main": "hello_world.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  }
}
